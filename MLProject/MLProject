name: diabetes-training

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      model_name:
        type: string
        default: "diabetes_model_random_forest"
    command: "python modelling.py"

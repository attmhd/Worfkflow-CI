name: diabetes-training

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      - name: model_name
        type: string
        default: "diabetes_model"
    command: "python modelling.py"
